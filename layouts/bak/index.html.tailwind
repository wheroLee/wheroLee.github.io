
{{ define "main" }}
<section class="intro text-center py-12 bg-gray-50 rounded-lg shadow-md">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">{{ .Site.Title }}</h1>
  <p class="text-lg text-gray-600 mb-6">{{ .Site.Params.description }}</p>

  <div class="about max-w-2xl mx-auto bg-white p-6 rounded-xl shadow">
    <h2 class="text-2xl font-semibold mb-3">👋 소개</h2>
    <p class="text-gray-700 leading-relaxed">
      안녕하세요! 제 블로그에 오신 것을 환영합니다.<br>
      연구 기록과 개인적인 생각을 공유하는 공간입니다.!!!!!!!!
    </p>
  </div>
</section>

<section class="recent-posts mt-12 max-w-4xl mx-auto">
  <h2 class="text-3xl font-bold mb-6 border-b pb-2">📝 최신 글</h2>
  <ul class="space-y-3">
    {{ range first 5 (where .Site.RegularPages "Type" "posts") }}
      <li class="hover:bg-gray-50 p-2 rounded transition">
        <a href="{{ .RelPermalink }}" class="text-blue-600 font-medium hover:underline">{{ .Title }}</a>
        <small class="text-gray-500 ml-2">({{ .Date.Format "2006-01-02" }})</small>
      </li>
    {{ end }}
  </ul>

  <h2 class="text-2xl font-semibold mt-10 mb-4">📌 오늘의 메모</h2>
  <ul class="list-disc pl-5 space-y-2 text-gray-700">
    {{ range first 5 (where .Site.RegularPages "Section" "memo") }}
      <li>
        <a href="{{ .RelPermalink }}" class="hover:text-blue-600">{{ .Title }}</a>
        <span class="text-gray-500 text-sm">({{ .Date.Format "2006-01-02" }})</span>
      </li>
    {{ end }}
  </ul>

  <h2 class="text-2xl font-semibold mt-10 mb-4">🔬 연구 기록</h2>
  <ul class="list-disc pl-5 space-y-2 text-gray-700">
    {{ range first 5 (where .Site.RegularPages "Section" "research") }}
      <li>
        <a href="{{ .RelPermalink }}" class="hover:text-blue-600">{{ .Title }}</a>
        <span class="text-gray-500 text-sm">({{ .Date.Format "2006-01-02" }})</span>
      </li>
    {{ end }}
  </ul>

  <h2 class="text-2xl font-semibold mt-10 mb-4">📚 블로그 글</h2>
  <ul class="list-disc pl-5 space-y-2 text-gray-700">
    {{ range first 5 (where .Site.RegularPages "Section" "posts") }}
      <li>
        <a href="{{ .RelPermalink }}" class="hover:text-blue-600">{{ .Title }}</a>
        <span class="text-gray-500 text-sm">({{ .Date.Format "2006-01-02" }})</span>
      </li>
    {{ end }}
  </ul>
</section>
{{ end }}


